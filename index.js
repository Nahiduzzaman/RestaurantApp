/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './src/root/App';
import {name as appName} from './src/root/App.json';

AppRegistry.registerComponent(appName, () => App);
